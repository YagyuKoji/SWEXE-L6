<% @users.each do |u| %>
    uid : <%= u.uid %> /
    pass : <%= u.pass %>
    <%= link_to '削除', user_path(u.id), method: :delete %>
    <%= link_to '編集', edit_user_path(u.id) %>
    <br>
<% end %>
    

<%= link_to '登録', new_user_path %>
<%= link_to 'ツイートを表示', tweets_path %>

